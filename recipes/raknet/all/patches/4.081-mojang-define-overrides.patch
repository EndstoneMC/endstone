Mojang RakNet define overrides (protocol version 11, 20 internal IDs)

--- a/Source/RakNetDefinesOverrides.h
+++ b/Source/RakNetDefinesOverrides.h
@@ -11,1 +11,2 @@
 // USER EDITABLE FILE
+#define MAXIMUM_NUMBER_OF_INTERNAL_IDS 20
--- a/Source/RakNetVersion.h
+++ b/Source/RakNetVersion.h
@@ -17,3 +17,3 @@
 // What compatible protocol version RakNet is using. When this value changes, it indicates this version of RakNet cannot connection to an older version.
 // ID_INCOMPATIBLE_PROTOCOL_VERSION will be returned on connection attempt in this case
-#define RAKNET_PROTOCOL_VERSION 6
+#define RAKNET_PROTOCOL_VERSION 11
